const SnakeView = require("SnakeView");

cc.Class({
    extends: SnakeView,

    properties: {
        
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    start () {

    },

    init(posX, posY, dir) {
        this._super(posX, posY, dir);
    },

    // update (dt) {},
});
